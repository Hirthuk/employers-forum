# Loaded automatically by Vite in production builds (`npm run build`, mode=production).
# This is what Netlify uses when building the deployed site.
# Netlify's own dashboard env var (if set) takes priority over this file at build time.
VITE_API_BASE_URL=https://insiderhub-backend.onrender.com
